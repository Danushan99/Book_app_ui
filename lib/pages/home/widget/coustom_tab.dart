import 'package:flutter/material.dart';

class CoustomTab extends StatelessWidget {
  const CoustomTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
